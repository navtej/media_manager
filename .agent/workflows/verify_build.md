---
description: Verify the project build status
---
1. Run the build command to check for errors.
   `flutter build macos --debug 2>&1 | head -100`

2. If the build fails, examine the output and fix the errors.
